    {% extends 'base.html.twig' %}

{% block title %}Supprimer un chaton{% endblock %}

{% block body %}
    <h1>Supprimer <i> {{ proprietaire.prenom }} {{ proprietaire.nom }} </i> ? </h1>
    {{ form(formulaire) }}
    {# Bouton pour annulé la suppression et retourner au menu #}
    <a href="{{ path("app_proprietaires") }}" class="btn btn-default">Retour</a>
{% endblock %}
